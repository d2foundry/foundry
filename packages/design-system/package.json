{
  "name": "@foundry/design-system",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "main": "./src/index.ts",
  "scripts": {
    "codegen": "panda codegen --clean",
    "build": "tsup src/index.ts --dts --format=esm,cjs --clean --dts --external @pandacss/dev",
    "dev": "pnpm build --watch"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@radix-ui/colors": "^3.0.0",
    "pandacss-preset-radix-colors": "^0.1.6",
    "ts-pattern": "^5.0.6"
  },
  "devDependencies": {
    "@foundry/eslint-config": "workspace:*",
    "@foundry/typescript-config": "workspace:*",
    "@pandacss/dev": "^0.21.0",
    "@pandacss/types": "^0.21.0",
    "@types/node": "^17.0.12",
    "tsup": "^7.2.0",
    "typescript": "^4.5.3"
  }
}
