{
  "name": "@foundry/panda-preset",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "main": "./src/index.ts",
  "scripts": {
    "codegen": "panda codegen --clean",
    "build": "tsup src/index.ts --dts --format=esm,cjs --clean --dts --external @pandacss/dev",
    "dev": "pnpm build --watch"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@foundry/eslint-config-custom": "workspace:*",
    "@foundry/tsconfig": "workspace:*",
    "@pandacss/dev": "^0.15.4",
    "@pandacss/types": "^0.15.4",
    "@types/node": "^17.0.12",
    "tsup": "^7.2.0",
    "typescript": "^4.5.3"
  }
}
